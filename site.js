function setMinimumDate(){const date=document.querySelector('#depart');if(date){date.min=new Date().toISOString().split('T')[0];}}
function selectTrip(button){document.querySelectorAll('.trip-tabs button').forEach(item=>item.classList.remove('active'));button.classList.add('active');}
function runSearch(){const from=document.querySelector('#from')?.value||'Manila (MNL)';const to=document.querySelector('#to')?.value||'Tokyo (NRT)';const date=document.querySelector('#depart')?.value;const details=encodeURIComponent(`${from} to ${to}${date?` on ${date}`:''}`);window.location.href=`manage.html?search=${details}`;}
function showBooking(){const reference=document.querySelector('#reference').value.trim().toUpperCase();const surname=document.querySelector('#surname').value.trim();const result=document.querySelector('#booking-result');if(!reference||!surname){result.textContent='Enter both your booking reference and last name to continue.';result.style.display='block';result.style.background='#fff1df';result.style.color='#8a4d00';return;}result.textContent=`Booking ${reference} found for ${surname}. Your AEROVIA demo itinerary is ready to view.`;result.style.display='block';result.style.background='#e6f5eb';result.style.color='#175b37';}
document.addEventListener('DOMContentLoaded',()=>{setMinimumDate();const params=new URLSearchParams(window.location.search);const search=params.get('search');if(search){const hint=document.querySelector('#search-hint');if(hint)hint.textContent=`You searched for ${search}. Use a sample reference such as AER123 to view a demo itinerary.`;}});
